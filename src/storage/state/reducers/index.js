import appReducer from '../app/appReducer';
import layoutReducer from '../layout/layoutReducer';
import homeReducer from '../home/homeReducer';

const reducers = {appReducer, layoutReducer, homeReducer};

export default reducers;
